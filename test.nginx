
server {
    server_name stream.moodyrahman.com;

    location / {
        proxy_pass http://100.88.142.123:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";

        # Streaming optimizations
        proxy_request_buffering off;
        proxy_cache_bypass $http_upgrade;

        # Prevent connection drops
        proxy_read_timeout 4s;
        proxy_send_timeout 4s;
    	proxy_connect_timeout 1s;
        send_timeout 4s;
    }



    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/stream.moodyrahman.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/stream.moodyrahman.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}